import { ModalTitle } from "gitgud";

export const Default = () => (
  <div className="glass r-xl p-6" style={{ width: 380 }}>
    <ModalTitle>Deploy new quest</ModalTitle>
    <p className="text-sm text-ink2">The heading style used across every kit modal.</p>
  </div>
);
